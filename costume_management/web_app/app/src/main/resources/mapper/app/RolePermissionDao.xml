<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.harukaze.costume.app.dao.RolePermissionDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.harukaze.costume.app.entity.RolePermissionEntity" id="rolePermissionMap">
        <result property="roleId" column="role_id"/>
        <result property="permissionId" column="permission_id"/>
    </resultMap>

    <select id="selectPermissionList" resultType="PermissionEntity">
        SELECT p.id, p.path, p.name
        FROM hk_permission p, hk_role_permission rp
        WHERE #{roleId} = rp.role_id AND rp.permission_id = p.id
    </select>
</mapper>